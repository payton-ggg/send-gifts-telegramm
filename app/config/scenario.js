module.exports = {
  // Target user username (without @) or phone number
  target: "username_or_phone",

  // Gift details
  gift: {
    count: 5, // Total hearts to send
  },

  // Delays between actions (ms)
  delays: {
    min: 2000,
    max: 5000,
  },
};
