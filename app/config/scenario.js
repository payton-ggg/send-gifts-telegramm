module.exports = {
  // Target user username (without @) or phone number
  target: "sn3ovzx",

  // Gift details
  gift: {
    count: 6, // Total hearts to send
  },

  // Delays between actions (ms)
  delays: {
    min: 1000,
    max: 2000,
  },
};
