module.exports = {
  // Target user username (without @) or phone number
  target: "guilemt",

  // Gift details
  gift: {
    count: 1, // Total hearts to send
  },

  // Delays between actions (ms)
  delays: {
    min: 1000,
    max: 2000,
  },
};
